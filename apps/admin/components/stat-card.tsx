import {Card,CardContent} from './ui/card'; import {ArrowUpRight,ArrowDownRight} from 'lucide-react'; import {ReactNode} from 'react'
export function StatCard({label,value,change,icon,negative=false}:{label:string,value:string,change:string,icon:ReactNode,negative?:boolean}){return <Card className="overflow-hidden"><CardContent className="p-5"><div className="flex items-start justify-between"><div className="grid h-10 w-10 place-items-center rounded-xl bg-primary/10 text-primary">{icon}</div><span className={'flex items-center gap-1 text-xs font-medium '+(negative?'text-red-500':'text-emerald-600')}>{negative?<ArrowDownRight size={13}/>:<ArrowUpRight size={13}/>} {change}</span></div><div className="mt-5 text-2xl font-bold tracking-tight">{value}</div><div className="mt-1 text-xs text-muted-foreground">{label}</div></CardContent></Card>}
