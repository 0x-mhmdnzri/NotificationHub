'use client'
import {useState} from 'react'; import {motion} from 'framer-motion'; import {MoreHorizontal,Search,Filter,ChevronDown} from 'lucide-react'; import {PageHeader,CreateButton} from './page-header'; import {Button} from './ui/button'; import {Input} from './ui/input'; import {Badge} from './ui/badge'; import {Card,CardContent} from './ui/card'; import {Table,TableHeader,TableBody,TableRow,TableHead,TableCell} from './ui/table'
export type Row={id:string; cells:string[]; status?:'success'|'warning'|'danger'|'default'}
export function ResourcePage({eyebrow,title,description,createLabel='Create',columns,rows,searchPlaceholder='Search...',filters=[]}:{eyebrow:string,title:string,description:string,createLabel?:string,columns:string[],rows:Row[],searchPlaceholder?:string,filters?:string[]}){const [q,setQ]=useState('');const visible=rows.filter(r=>r.cells.join(' ').toLowerCase().includes(q.toLowerCase()));return <div className="grid-bg min-h-full p-5 md:p-8"><div className="mx-auto max-w-[1500px]"><PageHeader eyebrow={eyebrow} title={title} description={description} action={<CreateButton>{createLabel}</CreateButton>}/><Card><CardContent className="p-0"><div className="flex flex-col gap-3 border-b p-4 md:flex-row"><div className="relative flex-1"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground"/><Input value={q} onChange={e=>setQ(e.target.value)} placeholder={searchPlaceholder} className="pl-9"/></div>{filters.map(f=><Button key={f} variant="outline" className="justify-between md:w-36"><span className="flex items-center gap-2"><Filter size={14}/>{f}</span><ChevronDown size={14}/></Button>)}</div><Table><TableHeader><TableRow>{columns.map(c=><TableHead key={c}>{c}</TableHead>)}<TableHead className="w-12"/></TableRow></TableHeader><TableBody>{visible.map((r,i)=><motion.tr key={r.id} initial={{opacity:0,y:5}} animate={{opacity:1,y:0}} transition={{delay:i*.025}} className="border-b transition hover:bg-muted/40">{r.cells.map((c,j)=><TableCell key={j} className={j===0?'font-medium':''}>{j===2&&r.status?<Badge variant={r.status}>{c}</Badge>:c}</TableCell>)}<TableCell><Button size="icon" variant="ghost"><MoreHorizontal size={16}/></Button></TableCell></motion.tr>)}</TableBody></Table></CardContent></Card></div></div>}
