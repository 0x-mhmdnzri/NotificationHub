# Build fix summary (2026-08-25)

## Root causes
1. `ActivityService` used `NotificationId` on entity (property is `Id`)
2. `RedisInboxEventBus` ambiguous `Channel` (Redis vs System.Threading.Channels)
3. Missing `FindCollapseAsync` — use `_statusStore.FindByCollapseKeyAsync`
4. Digest `Select` type inference on `JsonElement?`
5. SSE endpoint lambda missing return `IResult`
6. `ForwardedHeadersOptions` needed `using Microsoft.AspNetCore.HttpOverrides`
7. Plugin helpers `internal` but tests need `public`
8. `RenderedNotification.NotificationId` required in certification test
9. `ApiKeyHasher.GeneratePlainKey()` Substring when base64url < 40 chars
10. Outbox test expected SaveChanges after staged `AddAsync`
11. Newtonsoft.Json 12.0.3 transitive High vuln → pin 13.0.3

## Verification
- `dotnet build NotificationHub.sln -c Release` → 0 errors
- `dotnet test` → 131 passed
